---
import type { ContentType } from "contentful";
import ContentfulComponent from "contentful-astro/ContentfulComponent.astro";

let  {
  behaviour,
  title,
  heading,
  linkUrl,
  linkText,
  contentBlocks,
  parent,
  parentStyle,
} = Astro.props;

let component = "slider"

switch(behaviour) {
  case "Belt":
    // code block
    break;
  case "Gallery":
    // code block
    break;
  default:
    // code block
}
---
<h3 class="collectionTitle">
  {title}
</h3>
<h4 class="collectionHeading">
  {heading}
</h4>

<ContentfulComponent
type={component}
{...Astro.props}
/>


<style>
  .collectionTitle {
    @apply text-2xl md:text-3xl lg:text-4xl lg:leading-tight font-bold px-5 
  }
  .collectionHeading {
    @apply text-lg md:text-xl lg:text-2xl lg:leading-tight px-5 
  }
</style>