---
import * as contentful from 'contentful';
import { documentToHtmlString } from '@contentful/rich-text-html-renderer';

const {imageTitle, imageCaption, imageMedia} = Astro.props;

---
<figure>
	<img class="w-full" src={imageMedia.fields.file.url} alt={imageCaption} />
  </figure>