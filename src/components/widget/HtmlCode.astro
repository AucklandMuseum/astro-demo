---
import * as contentful from "contentful";
import { documentToHtmlString } from "@contentful/rich-text-html-renderer";

let { htmlContent, parent, parentStyle } = Astro.props;
---

<div class:list={["prose"]} set:html={htmlContent} />

<style>
	figure {
		@apply p-0 align-baseline block px-2.5 float-left clear-left ml-1 mt-1 mr-3 mb-0 max-w-[400px] w-1/3;
	}
</style>
